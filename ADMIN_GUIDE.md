# คู่มือผู้ดูแลระบบ — Happy HIP DVT Monitor (Web App v3.0.1)

เอกสารนี้สำหรับ superadmin/admin ใช้อ้างอิงตอนเพิ่ม/แก้/ลบ ข้อมูลในระบบ — ไม่ต้องจำ ค้นด้วย Ctrl+F ได้เลย

---

## 1. โครงสร้างข้อมูลโดยรวม

```
hospitals/
  {hid}/                    ← รหัสโรงพยาบาล เช่น "huahin"
    meta/name               ← ชื่อโรงพยาบาลที่แสดงผล
    beds/
      {bid}/                ← รหัสเตียง ต้องเป็นรูปแบบ BED-01, BED-02 ... เท่านั้น
        live/                  ข้อมูลสด จาก gateway เขียนเอง — ห้ามแก้มือ
        config/                ตั้งค่าเตียง — staff/admin/superadmin แก้ได้
        meta/                  ข้อมูลเคส/ฮาร์ดแวร์ — admin/superadmin เท่านั้น
        alerts/                แจ้งเตือน — เขียนได้เฉพาะ gateway/superadmin
        ack/                   การรับทราบแจ้งเตือนของ staff
        commands/              คำสั่งจากเว็บถึง gateway (เช่น ขอสอบเทียบ)
```

**สิทธิ์ตาม role** (กำหนดโดย `database.rules.json`):

| Role | เห็นกี่ รพ. | แก้ `config` | แก้ `meta` | เพิ่ม รพ./เตียง | ลบ รพ./เตียง |
|---|---|---|---|---|---|
| `superadmin` | ทุก รพ. | ✅ | ✅ | ✅ | ✅ |
| `admin` | รพ.ตัวเองเท่านั้น | ✅ | ✅ | ✅ (รพ.ตัวเอง) | ❌ |
| `staff` | รพ.ตัวเองเท่านั้น | ✅ | ❌ | ❌ | ❌ |
| `device` (gateway) | เฉพาะเตียงตัวเอง | เขียน `live`/`alerts` เท่านั้น | ❌ | ❌ | ❌ |

---

## 2. จัดการโรงพยาบาล (เฉพาะ superadmin)

**เพิ่ม**: แท็บ "ผู้ดูแลระบบ" → "＋ เพิ่มโรงพยาบาล" → ใส่ชื่อที่แสดงผล + รหัส `hid`
- `hid` ต้องเป็น**ตัวพิมพ์เล็ก ไม่มีเว้นวรรค** เช่น `huahin`, `prachuap`, `phetchaburi` — ไม่มีการบังคับรูปแบบจากระบบ ต้องระวังเองให้สอดคล้องกับของเดิม

**แก้ชื่อ**: กด "แก้ชื่อ" ข้างรายชื่อ รพ. ในแท็บผู้ดูแลระบบ (แก้ได้แค่ชื่อที่แสดงผล ไม่แก้ `hid` ได้ — ถ้าตั้ง `hid` ผิดต้องลบแล้วสร้างใหม่)

**ลบ**: กด "ลบ" → ต้องพิมพ์ `hid` ให้ตรงเพื่อยืนยัน — **ลบถาวร ไม่มีกู้คืน** ข้อมูลเตียง/ประวัติ/แจ้งเตือนทั้งหมดในโรงพยาบาลนั้นหายไปด้วย เฉพาะ **superadmin** เท่านั้นที่ลบได้

---

## 3. จัดการเตียง (เพิ่ม/แก้: admin/superadmin — ลบ: superadmin เท่านั้น)

**เพิ่ม**: แท็บ "ผู้ดูแลระบบ" → "＋ เพิ่มเตียง" → รหัสต้องเป็น `BED-01`, `BED-02` ... (2 หลัก) เท่านั้น ระบบบังคับรูปแบบนี้จริงจาก `database.rules.json`

**แก้ไข**: กด "แก้ไข" ข้างรายชื่อเตียง จะเห็นฟอร์มแบ่ง 2 กลุ่ม (อธิบายแต่ละช่องในหัวข้อ 4-5 ด้านล่าง) — admin แก้ได้ (staff แก้ `meta` ไม่ได้)

**ลบ**: กด "ลบ" → พิมพ์รหัสเตียงยืนยัน — **ลบถาวร** เหมือนกัน — **ปุ่มนี้ superadmin เท่านั้นที่เห็น** (admin จะไม่เห็นปุ่มลบเลย)

> ⚠️ **ทำไมลบได้แค่ superadmin**: การลบทั้งเตียงต้องลบข้อมูล `live`/`alerts` ไปด้วย ซึ่งปกติเขียนได้แค่ gateway/superadmin เท่านั้น — ถ้าให้สิทธิ์ admin ลบได้ Firebase จะให้สิทธิ์เขียน `live`/`alerts` ตรงๆ แก่ admin ไปด้วยโดยไม่ตั้งใจ (กลไก rules cascading) จึงจำกัดไว้ที่ superadmin เพื่อไม่เปิดช่องโหว่นี้

---

## 4. ความหมายของฟิลด์ในกลุ่ม "การตั้งค่าเตียง" (`config`)

| ฟิลด์ในฟอร์ม | ชื่อจริงใน Firebase | ความหมาย | ช่วงค่าที่ใช้ได้ |
|---|---|---|---|
| ชื่อเตียงที่แสดงบนจอ/เว็บ | `bedLabel` | ข้อความแทน BED-XX บนหน้าจอ เช่น "เตียง 1 วอร์ดชาย" | ≤40 ตัวอักษร |
| ชื่อวอร์ด/หอผู้ป่วย (config) | `wardLabel` | ใช้แสดงผลในเว็บเท่านั้น | ≤60 ตัวอักษร |
| เป้าหมาย Pump ต่อข้าง/วัน | `pumpTargetPerSide` | จำนวนครั้งกระดกข้อเท้าต่อ**ข้าง**ที่ตั้งเป็นเป้า — เว็บคูณ 2 เป็นเป้ารวมซ้าย+ขวาเอง | 0-2000 |
| มุมสะโพกที่เริ่มเตือน | `hipThresholdDeg` | ถ้ามุมสะโพกเกินค่านี้ ระบบเริ่มแจ้งเตือน | 30-120 (องศา) |
| ความถี่เตือนซ้ำ | `reminderIntervalMin` | ถ้ายังไม่กระดกข้อเท้า เตือนซ้ำทุกกี่นาที | 5-480 |
| ปิดเสียงเตือน ตั้งแต่/ถึงชม. | `quietHoursStart`/`quietHoursEnd` | ช่วงเวลากลางคืนที่ปิดเสียงเตือนอัตโนมัติ | 0-23 |
| เปิด/ปิดสั่นที่ข้อเท้า | `vibrationEnabled` | แก้ได้ที่แท็บ "แดชบอร์ด" (ปุ่มสวิตช์ ไม่ได้อยู่ในฟอร์มแก้ไขเตียง) | เปิด/ปิด |
| เปิด/ปิดเสียงที่หัวเตียง | `audioEnabled` | แก้ได้ที่แท็บ "แดชบอร์ด" เช่นกัน | เปิด/ปิด |

---

## 5. ความหมายของฟิลด์ในกลุ่ม "ข้อมูลเคส/ฮาร์ดแวร์" (`meta`)

⚠️ กลุ่มนี้แก้ได้เฉพาะ **admin/superadmin** เท่านั้น (staff แก้ไม่ได้เลย เพราะเป็น rules บังคับ)

| ฟิลด์ในฟอร์ม | ชื่อจริงใน Firebase | ความหมาย | หมายเหตุ |
|---|---|---|---|
| แผนก/วอร์ด (meta) | `ward` | ชื่อแผนก/วอร์ด | ⚠️ **ยังไม่ยืนยันว่าต่างจาก `wardLabel` ใน config ยังไง** — ดูเหมือนซ้ำซ้อนกัน ปัจจุบันกรอกได้ทั้งคู่แต่ไม่รู้ว่าฝั่งไหนถูกใช้แสดงผลจริงในระบบอื่น (เช่น รายงาน) |
| วันที่ผ่าตัด | `surgeryDate` | วันที่ผู้ป่วยเข้ารับการผ่าตัด | รูปแบบ `YYYY-MM-DD` เท่านั้น (เช่น 2026-08-23) |
| วิธีผ่าตัด | `approach` | แนวทางการผ่าตัดข้อสะโพกเทียม | เลือกได้แค่ 3 แบบ: ด้านหน้า (anterior) / ด้านหลัง (posterior) / ด้านข้าง (lateral) |
| MAC โหนด HIP/ankleL/ankleR | `nodeMacs.hip`/`.ankleL`/`.ankleR` | MAC address ของเซนเซอร์แต่ละตัวที่ผูกกับเตียงนี้ | รูปแบบ `XX:XX:XX:XX:XX:XX` เช่น `90:70:69:0B:DE:90` — ดูได้จาก serial log ตอน boot gateway (`[id] gateway MAC ...`) หรือฉลากบนตัวโหนด |

---

## 6. เพิ่ม/ปิดบัญชีผู้ใช้ (staff/admin/superadmin login)

**⚠️ ทำผ่านเว็บไม่ได้ — ต้องรันจาก Apps Script editor เท่านั้น** (ตั้งใจ เพราะ PIN เป็นความลับที่ไม่ควรผ่านหน้าเว็บ)

### เพิ่มบัญชีใหม่
เปิด `Code.gs` ใน Apps Script editor → วางฟังก์ชันชั่วคราวท้ายไฟล์ → เลือกจาก dropdown → Run → **ลบทิ้งหลังรันเสร็จ**:
```js
function addStaffTemp() {
  hhAddStaff('email@example.com', 'PIN 4-6 หลัก', 'admin', 'huahin', 'ชื่อที่แสดงผล');
  // role: 'superadmin' | 'admin' | 'staff'
  // hid: รหัส รพ. (ปล่อยว่าง '' ถ้าเป็น superadmin)
}
```

### ปิด/เปิดบัญชี (ไม่ลบ แค่ระงับชั่วคราว)
```js
function toggleStaffTemp() {
  hhSetStaffActive('email@example.com', false); // false = ปิด, true = เปิดกลับ
}
```

### ตรวจสอบว่ามีบัญชีอะไรบ้าง
เปิด Google Sheet (ID ใน `HH_SHEET_ID` ต้นไฟล์ `Code.gs`) → แท็บ **"Staff"** — ดูได้ตรงๆ (PIN จะเป็น hash ไม่ใช่ค่าจริง อ่านไม่ออกว่า PIN คืออะไร ต้องตั้งใหม่ถ้าลืม)

---

## 7. ปัญหาที่เจอบ่อย + วิธีแก้เบื้องต้น

| อาการ | สาเหตุที่เจอบ่อยที่สุด | วิธีเช็ค |
|---|---|---|
| Login ไม่ได้ ขึ้น "อีเมลหรือ PIN ไม่ถูกต้อง" | พิมพ์ผิด หรือบัญชีถูกปิด (`Active`=false ใน Staff sheet) | เช็คแท็บ Staff ว่า `Active`=TRUE |
| Login ไม่ได้ ขึ้น "พยายามผิดหลายครั้งเกินไป" | ใส่ PIN ผิด 5 ครั้งติด ระบบล็อก 15 นาทีอัตโนมัติ | รอ 15 นาทีแล้วลองใหม่ |
| Dashboard ว่างเปล่าทั้งที่ควรมีเตียง | Firebase rules ปฏิเสธการอ่าน (มักเกิดหลังแก้ rules ใหม่) | เปิด DevTools Console เช็ค error |
| แท็บ "รายงาน" ไม่มีข้อมูล | bed ID ที่เลือกไม่ตรงกับที่มีใน sheet `DailyHistory`, หรือ gateway ยังไม่เคย sync ประวัติมาเลย | เช็คแท็บ `DailyHistory` ใน Google Sheet ตรงๆ ว่ามีแถวของเตียงนั้นไหม |
| สอบเทียบกดแล้วไม่มีอะไรเกิดขึ้นจริง | gateway firmware ยังไม่มี listener รับคำสั่งนี้ (ทราบอยู่แล้ว เป็น open item) | ปุ่มเขียนคำสั่งเข้า Firebase ได้ปกติ แต่ฝั่งฮาร์ดแวร์ยังไม่ทำงานตาม |

---

*อัปเดตล่าสุด: 2026-08-23 พร้อม index.html v3.0.1*
